[작업플로우 정리 ]
>>> 로컬 WSIDFY브랜치에서 작업 후 로컬 dev브랜치랑 병합 진행하고 테스트 및 이상 없으면 원격 dev브랜치에 푸쉬

1. WSIDFY브랜치로 이동
git switch WSIDFY
2. 변경사항 스테이징 및 커밋
git add .
git commit -m "작업한 내용에 대한 메시지"
3. 로컬의 dev브랜치로 이동 및 원격최신 상태와 동기화
git switch dev
git pull origin dev (이미 연결 되어있으면 git pull만 진행)
4. WSIDFY 내용을 dev(로컬)에 merge
git merge WSIDFY (dev브랜치에서 실행해야함)
(이때 충돌나면 해결 후에 git add . > git commit을 해줘야 병합이 완료됨)
5. 원격 dev브랜치에 반영
git push origin dev


[깃 명령어 정리] ====================================
(연결된 레포지토리 해제)
git remote remove <원격_이름>

(원격 레포지토리와 연결)
git remote add <원격_이름> <원격_저장소_URL>
(ex. git remote add origin https://github.com/사용자이름/저장소이름.git)

(연결된 레포지토리 확인)
git remote -v

(기존 브랜치 해제)
git branch --unset-upstream

(원격 저장소 브랜치 확인)
git fetch

(브랜치 연결) - 기존에 커밋된 내용이 있어야 브랜치 연결 가능
git branch -u origin/<브랜치_이름>

(연결된 원격 브랜치 확인)
git branch -vv

(변경사항 브랜치에 올리기)
# 모든 변경사항 추가
git add . 
# 특정 파일만 추가
git add <파일명>

(커밋 생성)
git commit -m "커밋 메시지 입력"

(깃 푸쉬)
# 이미 연결(upstream)이 설정된 경우
git push
# 연결 설정과 동시에 푸시할 경우
git push -u origin <브랜치_이름>
# 강제로 푸시해야 할 경우 (주의 필요)
git push origin <브랜치_이름> -f

(깃 풀)
# 원격 저장소의 내용을 로컬로 덮어쓰기
1. 원격의 최신 상태 가져오기
git fetch --all

2. 원격 브랜치 내용을 로컬로 강제 리셋
git reset --hard origin/(브랜치명)

(수정 및 add된 파일 확인)
git status

(존재하는 모든 로컬 브랜치 확인)
git branch -a

(git switch로 브랜치 생성)
git switch -c development (develop브랜치 생성 및 전환)

(로컬 브랜치 삭제)
git branch -d foldering (-D는 강한 삭제)

(추적되지 않는 원격 브랜치를 보고있을 때 비워내기)
git fetch --prune

(로컬 브랜치 전환)
git switch (브랜치 명)

(원격 브랜치를 기준으로 새 브랜치 만들고 이동)
git switch -c feature/(새 브랜치) origin/(원격 브랜치 명)

+ (커밋별 내용 및 코드 확인)
git log --oneline --graph --all

(현재 작업진행도 임시저장) - stack구조
git stash (M - modify: 동일한 파일 구성 내에서 변경 코드만 추적해서 저장)
git stash -u (U - update : 추가로 생성된 파일까지 추적해서 저장)

(stash로 저장한 진행상황 불러오기) - 제일 최신순으로 적용
git stash pop

[가상 환경 구축 ] ====================================
(가상환경 만들기 - 공통)
python -m venv myvenv

(가상환경 실행하기 -  cmd)
myvenv\Scripts\activate

(가상환경 실행하기 - git bash)
source .venu/Scripts/activate (폴더명이 .venu)

(가상환경에서 나오기 - 공통)
deactivate

(가상환경 구동 중인지 확인)
which python 
> 이때 출력되는 경로 중간에 myvenv/Scripts/python가 포함된다면 가상환경 상태

[streamlit 실행 ] ====================================
(파일 실행)
streamlit run app.py

(특정 파일만 실행) - pages폴더의 test.py실행
cd pages (해당 폴더로 이동)
streamlit run .test.py (테스트 파일 실행)

[ VS code단축키 모음 ] ================================
(코드 잡고 통째로 옮기기)
원하는 열에 커서 두고 alt + 방향키(위/아래)

(폴더 전체에서 동일 변수명 한 번에 변경)
변수명 선택 + F12

(함수 혹은 클래스가 위치하는 파일로 바로 이동)
ctrl + 함수/클래스명 클릭

(여러 열에 동시입력)
alt + 입력할 행 선택, 타이핑

(다중 행 들여쓰기)
행 선택하고 ctrl + [ or ]

(다중 주석처리)
주석처리할 행 선택하고 ctrl + /


```
[업무관련 메모 ]
(DX사업부)
ㄴ>AX(AI전문조직)
ㄴ>CX(UI/UX 팀)
ㄴ>영업관리팀(영업조직) - 현재 자리 기준 좌측에 계신 분들
ㄴ> IT 기획팀(공공 사업무 기획 및 예산관리)

인턴기간 1/12 ~ 3/11

(공지사항)
문서중앙화(클라우드)로 로컬 파일이 저장되지 않음
가상 PC환경인 'DAAS'에서 웹 사용이 가능(외부 내부 분리되어있음)
   ㄴ> 따라서 망연계(외부 > 내부로 파일 받아올 때 승인이 필요)
부서간 직접 소통하는 업무도 배정 예정


(예정일정)-------------------------------------------------------------------------------
매달 출근부 작성 및 서명
월별 점심시간
   ㄴ> 1월(11:45) / 2월(11:25) / 3월(11:45)

(질문사항)-------------------------------------------------------------------------------
- ui수정사항



(주간목표)------------------------------------------------------------------------------
IT예산관리 대시보드 개발 마무리 및 컨펌받기

- 월 : 세부로직 구현 및 UI 다듬기 - 팀장님께 보고 후에 피드백
(추가 수정사항 및 UI 수정사항 파악)

[공통 ]
- 변수명 변경하기!
- 필터링 기능 옆에 조회 버튼을 하나 빼서 로딩 시간을 한 번으로 단축?
(각각의 필터링을 매길 때 마다 로딩되는 것 보다 빠른 느낌)
- 사이드바 추가기능 고민
1. 간단한 FAQ
2. 혹은 질문 남기기(질문이나 사용자로부터 모종의 요청이 왔을 때 1표시 같은거 뜨게 하기?)
3. 현재 로그인 된 계정 정보 표기
@피드백 : title삭제

[예산신청현황(총괄) ]
- 추후에 예시 데이터 지우기(현재 고정 출력이라 필터링 무시되는 중)
- 테이블 상단 칼럼 구조 점검 및 병합 처리(개선)
@피드백  : 데이터프레임 중앙정렬 / 데이터 프레임 컨테이너에 넣고 스크롤바 삽입

[부서별 집계 ]
@피드백 : 부서별 필터 3분할

[총괄표 ]
@피드백 : 중복값 최상단 출력으로 통일(부서명)

[자본예산 & 경비예산 ]
- 하단 입력란 필터링 후 부서명 변경시 위치를 찾지 못하는 현상
- 입력란의 전반적인 수정 필요
- 입력된 값을 다른 셀로 '옮기는'로직 구현 필요!!
@피드백 : 


(오늘 할 일)------------------------------------------------------------------------------

<민재>
[일반 유저(부서단위) - UI]
총괄표 부서명 중복값 최상단 출력 되도록
모달창 작업
- 목표 : 자본예산, 경비예산에 적용 가능하도록 수정하기

('전용 입력 모달창' 이슈 트래킹)
- 저장 및 닫기 버튼 작동하도록
- 적용 이력 주석처리
- 위에 라디오 버튼으로 '신규'항목일때는 B의 예산항목 및 세부분류를 텍스트 입력 창으로 변경
- 잔액 표기UI 수정
- 적용 연산했을 때 해당 기록이 '저장'버튼을 누르기 전까지는 축적되어 기록되도록 수정
- 전용 이력만 볼 수 있는 모달창 구현해서 적용
- 리스트 박스 좌표값 제대로 매칭되도록 수정(컨테이너 넣기)


2. 리스트 박스 아래에 '잔액'값 매칭 및 출력
3. 버튼 작동 플로우 구현
- 저장버튼 : 저장 > 모달 창 세션값 삭제 > 창 닫기
- 닫기버튼 : 모달 창 세션값 삭제 > 창 닫기
4,. '저장'시에 모달창의 세션값 기반 '전용 이력'남기는 로직 구현

df 컬럼 너비 조정 UI 고려

팀장님 피드백 사항

(자본/경비 탭 관련)
- 예산 주는 것만 구현
- 데이터 프레임의 높이 제한(컨테이너 height)풀기
- 우측 상단 창 닫기('X')활성화
- 금액 입력란 우측의 +/-버튼 제거 > 텍스트로 입력받고 정수로 변환 및 예외처리
- 받는 쪽(B)이 신규일 때 새로운 예산항목이 생성될 수 있도록 수정
> (신규는 입력받을 항목으로 '계정명', '예산항목','세부분류','추가업체' 항목 필요)
> 모달창 내부의 radio버튼을 통해 B컬럼의 예산항목, 세부분류를 텍스트 입력란으로 전환)
> 적용 후 최종 데이터 프레임에 추가된 내용의 행이 생성되어야 함
- 한 번에 예산항목 여러개를 생성받고 추가될 수 있도록 금액 입력란 아래에 기록 남기고 일괄 저장 구현
- 주는 쪽(A)을 여러개 할당해서 한 곳(B)으로 줄 수 있도록 UI개선

- 전용 컬럼 내에서 눌렀던 셀을 다시 눌렸을 때 모달창이 뜨지 않는 현상(개선)
+ rerun시에 깜빡임 처리(개선)
+ 전용입력 모달창 스크롤 안 되게 내용 구성(개선)

<영오>
===========진행 사항============

<완료>
[fix] 로그인 유지시간 time-UI 동기화
[Component] 쿠키 및 토큰 삭제,저장 로직 컴포넌트화
[UI update] 로그아웃 계정 옆으로
[UI update] DB 업데이트 시간 상단에 표기
[add] 현재 DB to xlsx 다운기능 추가

<🔥작업중> 

🔥[add] (자본,경비)예산 연산로직 mapping
[add] 총괄표 연산로직 mapping
[add] 예산 신청 현황 연산로직 mapping
[add] 로그인 계정에 따라 필터링 자동 적용
  - 소속 하위 부서만 필터링 셀렉트박스 목록에 나오도록
[add] upload 파일 양식샘플 다운기능 추가하기

<❌미완료>
[fix] DB 업데이트 시간 편집시간도 포함하기
[Component] admin_tab 내부 user_tab 동기화
===========추가고려사항=========
-(업데이트) 관리자가 임의로 사용자가 접속 못하도록 제한
-현재DB데이터 xlsx로 출력 하는 기능
. -(업데이트)그룹웨어에서 데이터 출력이 가능한지, 가능하다면 양식은?
-> 해당 양식을 알면 upload시 DB 업데이트 가능!
. 오늘 해당하는 년도를 제목에 포함시키기?
- 필터링 기능 옆에 조회 버튼을 하나 빼서 로딩 시간을 한 번으로 단축?
(각각의 필터링을 매길 때 마다 로딩되는 것 보다 빠른 느낌)

- (최종)코드 컴포넌트 화, 폴더 정리
- (최종)주석제거
===========❓Issue❓============
-계정은 어느단위까지 필요? 본부>부서>팀 + 부서직속 => 팀까지만!

-그룹웨어에서 데이터 출력이 가능한지, 가능하다면 양식은?-> 전달받음!

=========♨구현불가♨==========  
```
